// 这里使用redux-immutable的原因是 如果只是用redux的话他只支持js形式不支持immutable形式
// 所以使用redux-immutable
import { combineReducers } from 'redux-immutable'

// 将多个reducer合并 到index文件中引入
export default combineReducers({

})